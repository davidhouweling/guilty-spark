---
// Header/Navigation component
---

<nav class="halo-nav">
  <a href="/" class="nav-logo">
    <img src="/guilty-spark-h3.png" alt="Guilty Spark" />
    <span>Guilty Spark</span>
  </a>
  <div class="nav-links">
    <a href="/faq">FAQ</a>
    <a href="https://github.com/davidhouweling/guilty-spark" target="_blank">GitHub</a>
    <a href="https://discord.gg/WS7zG8GDKY" target="_blank">Support server</a>
  </div>
</nav>

<style>
  /* Navigation */
  .halo-nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    height: 70px;
    background: rgba(13, 31, 38, 0.95);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--halo-teal-dark);
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 3rem;
    z-index: 100;
  }

  .nav-logo {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-family: "Saira Condensed", sans-serif;
    font-size: 1.5rem;
    font-weight: 700;
    letter-spacing: 0.15em;
    color: var(--halo-teal-primary);
    text-shadow: 0 0 10px rgba(93, 212, 216, 0.5);
    text-transform: uppercase;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .nav-logo:hover {
    color: var(--halo-white);
    text-shadow: 0 0 15px rgba(93, 212, 216, 0.8);
  }

  .nav-logo img {
    width: 40px;
    height: 40px;
    filter: drop-shadow(0 0 8px rgba(93, 212, 216, 0.6));
  }

  .nav-links {
    display: flex;
    gap: 2rem;
  }

  .nav-links a {
    font-family: "Oxanium", sans-serif;
    font-size: 0.95rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: var(--halo-teal-primary);
    text-decoration: none;
    transition: all 0.3s ease;
    position: relative;
    text-transform: uppercase;
  }

  .nav-links a::after {
    content: "";
    position: absolute;
    bottom: -5px;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--halo-teal-primary);
    transition: width 0.3s ease;
  }

  .nav-links a:hover {
    color: var(--halo-white);
    text-shadow: 0 0 10px rgba(93, 212, 216, 0.8);
  }

  .nav-links a:hover::after {
    width: 100%;
    box-shadow: 0 0 10px var(--halo-teal-primary);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .halo-nav {
      padding: 0 1.5rem;
    }

    .nav-links {
      gap: 1rem;
    }

    .nav-links a {
      font-size: 0.85rem;
    }
  }
</style>
