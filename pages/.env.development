# Development environment variables
# API host for WebSocket connections
# local
# PUBLIC_API_HOST=localhost:8787
# production
PUBLIC_API_HOST=api.guilty-spark.app

# Allow for preview mode options in streamer view (for OBS overlay testing)
OBS_OVERLAY_PREVIEW=true
