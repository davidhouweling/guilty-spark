# Development environment variables
# API base URL for HTTP and WebSocket connections (include protocol)
# local
BACKEND_URL=http://localhost:8787
# production
# BACKEND_URL=https://api.guilty-spark.app

# Allow for preview mode options in streamer view (for OBS overlay testing)
OBS_OVERLAY_PREVIEW=true
